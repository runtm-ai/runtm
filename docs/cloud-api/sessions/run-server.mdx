---
title: "Run Server"
api: "POST /api/sessions/{session_id}/run-server"
description: "Start a development server in the sandbox"
---

Starts a development server process in the sandbox. The server runs in the background and its port becomes accessible via the sandbox URL.

**Required scope:** `sessions:write`

## Path Parameters

<ParamField path="session_id" type="string" required>
  The session ID (e.g. `ses_abc123`).
</ParamField>

## Headers

<ParamField header="Authorization" type="string" required>
  Bearer token. Example: `Bearer runtm_xxx`
</ParamField>

## Body

<ParamField body="command" type="string" required>
  Command to start the server (e.g. `npm run dev`).
</ParamField>

<ParamField body="port" type="integer" required>
  Port the server will listen on.
</ParamField>

## Response

<ResponseField name="session_id" type="string">
  Session ID.
</ResponseField>

<ResponseField name="command" type="string">
  Command that was started.
</ResponseField>

<ResponseField name="port" type="integer">
  Port the server is listening on.
</ResponseField>

<ResponseField name="url" type="string">
  Public URL to access the server.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://app.runtm.com/api/cloud/sessions/ses_abc123/run-server" \
  -H "Authorization: Bearer runtm_xxx" \
  -H "Content-Type: application/json" \
  -d '{"command": "npm run dev", "port": 3000}'
```

```python Python
import requests

response = requests.post(
    "https://app.runtm.com/api/cloud/sessions/ses_abc123/run-server",
    headers={
        "Authorization": "Bearer runtm_xxx",
        "Content-Type": "application/json",
    },
    json={"command": "npm run dev", "port": 3000},
)

data = response.json()
print(f"Server running at {data['url']}")
```

```javascript JavaScript
const response = await fetch(
  "https://app.runtm.com/api/cloud/sessions/ses_abc123/run-server",
  {
    method: "POST",
    headers: {
      "Authorization": "Bearer runtm_xxx",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({ command: "npm run dev", port: 3000 }),
  }
);

const data = await response.json();
console.log(`Server running at ${data.url}`);
```
</RequestExample>

<ResponseExample>
```json 200
{
  "session_id": "ses_abc123",
  "command": "npm run dev",
  "port": 3000,
  "url": "https://ses-abc123-3000.runtm.dev"
}
```
</ResponseExample>
