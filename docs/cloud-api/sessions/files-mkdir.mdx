---
title: "Create Directory"
api: "POST /api/sessions/{session_id}/file/mkdir"
description: "Create a directory in the sandbox"
---

Creates a directory at the specified path in the sandbox. Parent directories are created automatically if they don't exist.

**Required scope:** `sessions:write`

## Path Parameters

<ParamField path="session_id" type="string" required>
  The session ID (e.g. `ses_abc123`).
</ParamField>

## Headers

<ParamField header="Authorization" type="string" required>
  Bearer token. Example: `Bearer runtm_xxx`
</ParamField>

## Body

<ParamField body="path" type="string" required>
  Directory path to create (e.g. `/src/components`).
</ParamField>

## Response

<ResponseField name="path" type="string">
  Path of the created directory.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://app.runtm.com/api/cloud/sessions/ses_abc123/file/mkdir" \
  -H "Authorization: Bearer runtm_xxx" \
  -H "Content-Type: application/json" \
  -d '{"path": "/src/components"}'
```

```python Python
import requests

response = requests.post(
    "https://app.runtm.com/api/cloud/sessions/ses_abc123/file/mkdir",
    headers={
        "Authorization": "Bearer runtm_xxx",
        "Content-Type": "application/json",
    },
    json={"path": "/src/components"},
)

print(f"Created: {response.json()['path']}")
```

```javascript JavaScript
const response = await fetch(
  "https://app.runtm.com/api/cloud/sessions/ses_abc123/file/mkdir",
  {
    method: "POST",
    headers: {
      "Authorization": "Bearer runtm_xxx",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({ path: "/src/components" }),
  }
);

const data = await response.json();
console.log(`Created: ${data.path}`);
```
</RequestExample>

<ResponseExample>
```json 200
{
  "path": "/src/components"
}
```
</ResponseExample>
