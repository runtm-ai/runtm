---
title: "Start Session"
api: "POST /api/sessions/{session_id}/start"
description: "Cold-start a session that hasn't been started yet"
---

Cold-starts a session that was created but hasn't been started. Provisions the sandbox and transitions the session to `active`.

**Required scope:** `sessions:write`

## Path Parameters

<ParamField path="session_id" type="string" required>
  The session ID (e.g. `ses_abc123`).
</ParamField>

## Headers

<ParamField header="Authorization" type="string" required>
  Bearer token. Example: `Bearer runtm_xxx`
</ParamField>

<ParamField header="X-Organization-Id" type="string">
  Scope to an organization.
</ParamField>

## Response

Returns the updated session object with `state: "active"`.

<ResponseField name="id" type="string">
  Session ID.
</ResponseField>

<ResponseField name="state" type="string">
  Updated state (`active`).
</ResponseField>

<ResponseField name="sandbox_url" type="string">
  URL of the provisioned sandbox.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://app.runtm.com/api/sessions/ses_abc123/start" \
  -H "Authorization: Bearer runtm_xxx"
```

```python Python
import requests

response = requests.post(
    "https://app.runtm.com/api/sessions/ses_abc123/start",
    headers={"Authorization": "Bearer runtm_xxx"},
)

session = response.json()
print(f"Started: {session['sandbox_url']}")
```

```javascript JavaScript
const response = await fetch(
  "https://app.runtm.com/api/sessions/ses_abc123/start",
  {
    method: "POST",
    headers: { "Authorization": "Bearer runtm_xxx" },
  }
);

const session = await response.json();
console.log(`Started: ${session.sandbox_url}`);
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": "ses_abc123",
  "name": "API refactor",
  "state": "active",
  "agent": "claude-code",
  "sandbox_url": "https://ses-abc123.runtm.dev",
  "updated_at": "2026-05-09T21:35:00Z"
}
```
</ResponseExample>
