---
title: "Pause Session"
api: "POST /api/sessions/{session_id}/pause"
description: "Pause a session to save resources"
---

Pauses an active session. The sandbox is suspended and compute resources are released. Session state is preserved and can be resumed later.

**Required scope:** `sessions:write`

## Path Parameters

<ParamField path="session_id" type="string" required>
  The session ID (e.g. `ses_abc123`).
</ParamField>

## Headers

<ParamField header="Authorization" type="string" required>
  Bearer token. Example: `Bearer runtm_xxx`
</ParamField>

<ParamField header="X-Organization-Id" type="string">
  Scope to an organization.
</ParamField>

## Response

Returns the updated session object with `state: "paused"`.

<ResponseField name="id" type="string">
  Session ID.
</ResponseField>

<ResponseField name="state" type="string">
  Updated state (`paused`).
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://app.runtm.com/api/cloud/sessions/ses_abc123/pause" \
  -H "Authorization: Bearer runtm_xxx"
```

```python Python
import requests

response = requests.post(
    "https://app.runtm.com/api/cloud/sessions/ses_abc123/pause",
    headers={"Authorization": "Bearer runtm_xxx"},
)

print(response.json()["state"])  # "paused"
```

```javascript JavaScript
const response = await fetch(
  "https://app.runtm.com/api/cloud/sessions/ses_abc123/pause",
  {
    method: "POST",
    headers: { "Authorization": "Bearer runtm_xxx" },
  }
);

const session = await response.json();
console.log(session.state); // "paused"
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": "ses_abc123",
  "name": "API refactor",
  "state": "paused",
  "agent": "claude-code",
  "updated_at": "2026-05-09T21:25:00Z"
}
```
</ResponseExample>
