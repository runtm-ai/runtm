---
title: Introduction
description: "Sandboxes where coding agents build and deploy"
---

Sandboxes where coding agents build and deploy.

Spin up isolated environments where Claude Code, Cursor, Codex, and other agents code and ship software. With live URLs, logs, and previews.

```bash
runtm init
runtm deploy
```

<CardGroup cols={2}>
  <Card title="Quick Start" icon="rocket" href="/quickstart">
    Zero to deployed in 5 minutes
  </Card>
  <Card title="Cloud Platform" icon="cloud" href="/cloud/overview">
    Sandboxing, background agents, and telemetry
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/cli/overview">
    Complete command reference
  </Card>
  <Card title="API Reference" icon="code" href="/api/overview">
    Programmatic access
  </Card>
  <Card title="Background Agents" icon="robot" href="/cloud/background-agents">
    Launch autonomous agents via API
  </Card>
  <Card title="Self-Hosting" icon="server" href="/self-hosting/overview">
    Run on your own infrastructure
  </Card>
</CardGroup>

## Why Runtm?

AI coding agents like Claude Code and Cursor are powerful—but they need a place to run. Runtm gives them:

- **Isolated sandboxes** – Agents code with full permissions without touching your machine
- **Real URLs** – Deploy to live HTTPS endpoints instantly
- **Logs and previews** – See what your agent built, debug issues, iterate fast
- **Works with any agent** – Claude Code, Cursor, Codex, Gemini CLI, and more
- **Background agents via API** – Launch autonomous coding tasks programmatically ([learn more](/cloud/background-agents))
- **Telemetry** – Traces, metrics, and events for your deployments ([learn more](/cloud/telemetry))

## How it works

```mermaid
flowchart LR
    Agent[Your Agent] --> Sandbox[Sandbox] --> Runtm[Runtm] --> URL[Live URL]
    
    style Agent fill:#f4f4f5,stroke:#18181b,stroke-width:2px,color:#18181b
    style Sandbox fill:#f4f4f5,stroke:#18181b,stroke-width:2px,color:#18181b
    style Runtm fill:#f4f4f5,stroke:#18181b,stroke-width:2px,color:#18181b
    style URL fill:#18181b,stroke:#18181b,stroke-width:2px,color:#fafafa
```

1. **Spin up a sandbox** – Your agent gets an isolated workspace
2. **Agent builds** – Full permissions to code, install deps, run tests
3. **Deploy** – One command to a real URL with logs and previews
4. **Iterate** – Agent sees logs, fixes bugs, redeploys

## What you can build

Agents can build and deploy any web software:

- APIs and webhooks
- Dashboards and admin panels
- Landing pages and docs
- Automations and agent backends
- Full-stack web apps

<Info>
Runtm is **bring-your-own-cloud**. We start with Fly.io as the default provider, with AWS and GCP support coming soon.
</Info>

## Cloud providers

| Provider | Status |
|----------|--------|
| Fly.io | Available now |
| AWS | Coming soon |
| GCP | Coming soon |

<Note>
Self-hosters can implement custom providers using the provider interface.
</Note>

## Guardrails

Runtm enforces sensible limits by default:

| Constraint | Limit |
|------------|-------|
| Artifact size | 20 MB |
| Build timeout | 10 minutes |
| Deploy timeout | 5 minutes |
| Rate limit | 10 deploys/hour |

## Get started

<Steps>
  <Step title="Install the CLI">
    ```bash
    uv tool install runtm
    ```
  </Step>
  <Step title="Spin up a sandbox">
    ```bash
    runtm init
    ```
  </Step>
  <Step title="Deploy">
    ```bash
    runtm deploy
    ```
  </Step>
</Steps>
