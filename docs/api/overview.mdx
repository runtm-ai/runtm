---
title: "API Overview"
description: "Runtm REST API for programmatic access"
---

The Runtm API lets you programmatically spin up sandboxes, deploy, and manage applications. Use it to integrate Runtm into your agent workflows or automation pipelines.

## Base URL

```
https://app.runtm.com/api
```

For self-hosted instances, use your own API URL.

## Authentication

All API requests require a Bearer token in the `Authorization` header:

```bash
curl -H "Authorization: Bearer runtm_xxx" https://app.runtm.com/api/v0/deployments
```

Get your API key from [app.runtm.com](https://app.runtm.com) or create one via CLI:

```bash
runtm login
```

## API Scopes

API keys have specific permissions:

| Scope | Permissions |
|-------|-------------|
| `read` | List deployments, view logs, query telemetry |
| `deploy` | Create/update deployments, ingest telemetry (includes `read`) |
| `delete` | Destroy deployments (includes `read`) |
| `sessions` | Create/manage sandbox sessions, launch background agents |
| `admin` | Full access including token management |

## Endpoints

### Sessions (Cloud)

| Method | Endpoint | Description |
|--------|----------|-------------|
| `POST` | `/v0/sessions` | [Create session](/api/sessions/create) |
| `POST` | `/v0/sessions/launch` | [Launch background agent](/api/sessions/launch) |
| `GET` | `/v0/sessions/{id}` | [Get session / poll status](/api/sessions/get) |
| `POST` | `/v0/sessions/{id}/prompt` | [Send prompt (SSE)](/api/sessions/prompt) |
| `POST` | `/v0/sessions/{id}/git` | [Git operations](/api/sessions/git) |
| `DELETE` | `/v0/sessions/{id}` | [Delete session](/api/sessions/delete) |

### Deployments

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/v0/deployments` | [List deployments](/api/deployments/list) |
| `POST` | `/v0/deployments` | [Create deployment](/api/deployments/create) |
| `GET` | `/v0/deployments/{id}` | [Get deployment](/api/deployments/get) |
| `DELETE` | `/v0/deployments/{id}` | [Delete deployment](/api/deployments/delete) |
| `GET` | `/v0/deployments/search` | [Search deployments](/api/deployments/search) |

### Telemetry

| Method | Endpoint | Description |
|--------|----------|-------------|
| `POST` | `/v0/telemetry` | [Ingest batch](/api/telemetry/ingest) |
| `GET` | `/v0/telemetry/traces` | [List traces](/api/telemetry/traces) |
| `GET` | `/v0/telemetry/traces/{trace_id}` | [Get trace](/api/telemetry/traces) |
| `GET` | `/v0/telemetry/deployments/{id}/traces` | [Deployment traces](/api/telemetry/traces) |
| `GET` | `/v0/telemetry/metrics` | [List metrics](/api/telemetry/metrics) |
| `GET` | `/v0/telemetry/metrics/summary` | [Metrics summary](/api/telemetry/metrics) |
| `GET` | `/v0/telemetry/events` | [List events](/api/telemetry/events) |

### Logs

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/v0/deployments/{id}/logs` | [Get logs](/api/logs) |

### Domains

| Method | Endpoint | Description |
|--------|----------|-------------|
| `POST` | `/v0/deployments/{id}/domains` | [Add domain](/api/domains/add) |
| `GET` | `/v0/deployments/{id}/domains/{hostname}` | [Get domain status](/api/domains/get) |
| `DELETE` | `/v0/deployments/{id}/domains/{hostname}` | [Remove domain](/api/domains/remove) |

### User

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/v1/me` | [Get current user](/api/user/me) |

## Rate Limits

| Limit | Value |
|-------|-------|
| Deployments per hour | 10 |
| Max artifact size | 20 MB |
| Build timeout | 10 minutes |
| Deploy timeout | 5 minutes |

## Error Responses

Errors return JSON with an `error` field:

```json
{
  "detail": {
    "error": "Deployment not found",
    "deployment_id": "dep_invalid"
  }
}
```

Common HTTP status codes:

| Code | Meaning |
|------|---------|
| `400` | Invalid request (bad parameters) |
| `401` | Invalid or missing API key |
| `403` | Insufficient permissions |
| `404` | Resource not found |
| `409` | Conflict (e.g., invalid state) |
| `413` | Artifact too large |
| `429` | Rate limit exceeded |
| `500` | Server error |

## Idempotency

For `POST` requests, use the `Idempotency-Key` header to ensure safe retries:

```bash
curl -X POST "https://app.runtm.com/api/v0/deployments" \
  -H "Authorization: Bearer runtm_xxx" \
  -H "Idempotency-Key: unique-request-id-123" \
  -F "manifest=@runtm.yaml" \
  -F "artifact=@artifact.zip"
```

If the same key is used again, the API returns the existing deployment instead of creating a new one.

## SDKs

<CardGroup cols={2}>
  <Card title="Python" icon="python">
    ```bash
    pip install runtm
    ```
    Use the CLI module programmatically or call the REST API with `requests`
  </Card>
  <Card title="HTTP Client" icon="globe">
    Use any HTTP client — `curl`, `fetch`, `axios`, etc.
  </Card>
</CardGroup>

## Cloud Platform

The hosted platform at [app.runtm.com](https://app.runtm.com) adds sandbox sessions, background agents, and telemetry on top of the core deployment API. See the [Cloud Platform docs](/cloud/overview) for details.
